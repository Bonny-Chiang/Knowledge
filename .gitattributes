# Git LFS is intentionally not enabled by default.
# When ready, install Git LFS and uncomment patterns like these:
#
# raw/**/*.pdf filter=lfs diff=lfs merge=lfs -text
# raw/**/*.mp4 filter=lfs diff=lfs merge=lfs -text
# raw/**/*.mov filter=lfs diff=lfs merge=lfs -text
# raw/**/*.zip filter=lfs diff=lfs merge=lfs -text
# raw/**/*.tar filter=lfs diff=lfs merge=lfs -text
# raw/**/*.tar.gz filter=lfs diff=lfs merge=lfs -text

*.md text eol=lf
*.json text eol=lf
*.py text eol=lf

